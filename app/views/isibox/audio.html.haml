.cabecera
  .icono 
    =image_tag("icono.png")
  %p ISIBox
  #usuario
    %p Sonando #{@file.name}

#imagen
  =image_tag("musica.png")

#audio
  =audio_tag("#{@file.file}", :autodisplay => true, :controls => true)

.return
  =link_to= link_to "Volver página principal", isibox_index_path
